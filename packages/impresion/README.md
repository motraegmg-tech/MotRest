# @motrest/impresion — STUB

Impresión ESC/POS centralizada en el hub (TRD §8, ADR-08):
- Red **9100** (preferida), **USB** (`node-usb` en la caja), **Bluetooth** (Capacitor).
- Fallback: en modo isla cada cliente imprime directo a la impresora de red.

**Aún sin implementar.** Se construye junto con el KDS y el POS de F1.
