# Copia este archivo a `video/.env` y pega ahí las llaves.
# `video/.env` está ignorado por git: las llaves NUNCA van al repositorio.

# --- Voz: Gemini TTS (Google AI Studio) ---
# https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Musica: Suno via sunoapi.org ---
# Panel del proveedor -> API Key
SUNO_API_KEY=
SUNO_API_URL=https://api.sunoapi.org

