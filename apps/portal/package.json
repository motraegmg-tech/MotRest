{
  "name": "@motrest/portal",
  "version": "0.0.0",
  "private": true,
  "description": "Portal del comensal: la pagina a la que entra el cliente desde el QR de su ticket. Encuesta, reservas y su cuenta.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "svelte-check --tsconfig ./tsconfig.json",
    "lint": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "@motrest/dominio": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.1.1",
    "svelte": "^5.56.7",
    "svelte-check": "^4.7.3",
    "typescript": "^5.9.3",
    "vite": "^6.4.3"
  }
}
