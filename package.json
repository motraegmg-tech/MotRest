{
  "name": "motrest",
  "version": "0.0.0",
  "private": true,
  "description": "MotRest — ERP restaurantero de MOTRAE. Monorepo (F1: núcleo operativo).",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "dev:pos": "pnpm --filter pos-ui dev"
  },
  "comentarios": {
    "pnpm.overrides.tar": "Forzado a la línea 7 parchada. `tar` entra por @capacitor/cli (build del KDS Android, solo desarrollo) y arrastraba varias travesías de ruta. No está en el producto —las deps de producción del Hub son selfsigned y ws— pero se cierra igual."
  },
  "pnpm": {
    "overrides": {
      "tar": "^7.4.3"
    }
  }
}
